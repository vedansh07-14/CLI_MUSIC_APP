# @audio/mic-darwin-arm64

> Prebuilt native capture binary for [@audio/mic](https://github.com/audiojs/mic) — darwin-arm64. Installed automatically as an optionalDependency; never require this package directly.

If the binary is unavailable, @audio/mic falls back to its process/null backends.
